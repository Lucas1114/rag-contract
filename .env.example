# Embedding API key. Used once, by `rag-contract build-index`, whose output is
# committed. The eval runs off those committed vectors and needs no key.
OPENAI_API_KEY=

# Answer generation. Not used by the retrieval eval or by CI.
ANTHROPIC_API_KEY=

DAILY_SPEND_CAP_USD=2.00
